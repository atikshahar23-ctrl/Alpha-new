# LYRICS::TRANSLATOR — הפעלה ראשונית

> המערכת משולבת בפלטפורמת Alpha / Heavy Guard OS ורצה מ-GitHub Pages בכתובת
> `https://<domain>/Alpha-new/lyrics.html` — **אין צורך בשרת מקומי**. שלב 2
> המקורי (הרצה מקומית) נשמר כאן רק למקרה של פיתוח מקומי.

## שלב 1 — יצירת Spotify Client ID (חינם, ~2 דקות)

1. היכנס ל־[developer.spotify.com/dashboard](https://developer.spotify.com/dashboard) עם חשבון הספוטיפיי הרגיל שלך.
2. לחץ **Create app**.
3. מלא שם ותיאור חופשיים (למשל: `Lyrics Translator`).
4. בשדה **Redirect URIs** תצטרך להזין בדיוק את הכתובת שבה תריץ את הקובץ (ראה שלב 2 למטה) — אפשר לחזור לכאן אחרי שתדע אותה.
5. סמן **Web API** ברשימת ה-APIs, אשר את תנאי השימוש ולחץ **Save**.
6. בעמוד ההגדרות של האפליקציה שנוצרה תראה **Client ID** — העתק אותו, תצטרך אותו בשלב 3.

## שלב 2 — הרצת הקובץ מקומית

הדפדפן לא מאפשר ל-Spotify להפנות בחזרה לקובץ שנפתח ישירות (`file://`), אז צריך שרת מקומי קטן. הכי פשוט:

**אם יש לך Node.js מותקן:**
```
npx serve .
```

**אם יש לך Python:**
```
python3 -m http.server 5500
```

לאחר ההרצה, האתר יהיה זמין בכתובת שתקבל בטרמינל (למשל `http://127.0.0.1:5500` או `http://localhost:3000`).
פתח את הכתובת הזו בדפדפן.

> חשוב: Spotify דורש `127.0.0.1` ולא `localhost` עבור אפליקציות בפיתוח שאינן ב-HTTPS. אם קיבלת כתובת עם `localhost`, פשוט החלף ל-`127.0.0.1` באותה כתובת (למשל `http://127.0.0.1:5500`).

## שלב 3 — חיבור בתוך האפליקציה

1. פתח את הקובץ בדפדפן (לפי הכתובת משלב 2).
2. לחץ על סמל גלגל השיניים (⚙️) בפינה השמאלית העליונה.
3. הדבק את ה-**Client ID** שהעתקת בשלב 1.
4. העתק את הכתובת שמופיעה תחת **"Redirect URI לרישום"** בעזרת הכפתור "העתק".
5. חזור לדשבורד של Spotify (שלב 1), פתח **Edit Settings**, הדבק את הכתובת הזו בדיוק כפי שהיא בשדה **Redirect URIs**, ולחץ **Save**.
6. חזור לאפליקציה, לחץ **שמור** בפאנל ההגדרות, ואז **התחבר לספוטיפיי**.

מרגע זה, האפליקציה תזהה אוטומטית מה מתנגן אצלך בספוטיפיי ותציג את המילים המתורגמות.

## מצב ידני (ליוטיוב או כל מקור אחר)

בכל שלב אפשר להזין שם אמן + שם שיר ידנית בשדה "חיפוש ידני" בתחתית המסך העליון — זה לא דורש חיבור לספוטיפיי, ומתאים גם לשירים המתנגנים ביוטיוב.

## חשוב לדעת — מגבלת כיסוי המילים

האפליקציה מגיעה מוגדרת עם ספק מילים חינמי (ללא מפתח, ללא הרשמה) לצורך הדגמה. הספק הזה:
- **לא מכסה כל שיר** — הרבה שירים (במיוחד עבריים, נדירים, או חדשים) פשוט לא יימצאו.
- מחזיר טקסט גולמי בלבד, בלי תזמון מדויק ברמת מילה — הסנכרון במסך הוא הערכה לפי התקדמות השיר, לא תזמון אמיתי.

כדי לשפר את זה משמעותית, אפשר להירשם לשירות מילים בתשלום כמו **Musixmatch** (יש להם גם API עם תזמון synced-lyrics) או **Genius**, ולהחליף את הפונקציה `fetchRawLyrics` בקובץ `index.html` כך שתקרא לשירות החדש עם המפתח שלך. הפונקציה מבודדת בכוונה כדי שההחלפה תהיה קלה (בקובץ `lyrics.html` במאגר).

## החלפת מנוע התרגום

מנוע התרגום הנוכחי (`translateLine` בקובץ) משתמש בשירות MyMemory החינמי, שמוגבל בכמות בקשות ליום ולא תמיד מדויק. אם תרצה תרגום איכותי יותר, אפשר להחליף אותו בקריאה ל-Google Cloud Translation API או DeepL API (דורש מפתח בתשלום), באותה פונקציה.
